
add position recovery for collisions

improve collision detection to handle screen borders

change SIZES and SPEEDS to be relative to the screen resolution

make window resizable 

add a scoring condition

display score

add arguments to Spong_Run(), convert the appropriate macros to parameters

improve object initialization

add support for an arbitrary number of objects (multiple balls)
